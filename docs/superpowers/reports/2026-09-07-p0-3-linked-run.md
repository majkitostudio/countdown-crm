# P0.3 — první skutečný linked důkaz

**Datum:** 7. 9. 2026

**Stav:** úspěšně ověřeno

## Co bylo spuštěno

Runner byl spuštěn proti linked sandboxu `countdown-crm-sandbox-20260824`.
Použitá identita byla krátkodobý scoped Supabase Personal Access Token omezený
na tento projekt a oprávnění `Database: Read`. Hodnota tokenu ani celý project
ref nejsou v tomto reportu uvedené.

Příkaz:

```text
npm run verify:linked-security
```

Runner použil read-only Management API endpoint. Nebyl použit existující CLI
session, `SUPABASE_SECRET_KEY` ani `SUPABASE_SERVICE_ROLE_KEY`.

## Sanitizovaný výstup

```json
{
  "status": "passed",
  "target": "linked-sandbox",
  "mode": "read-only",
  "projectRefFingerprint": "5f7c220a1a60",
  "checks": {
    "total": 8,
    "passed": 8,
    "failed": []
  }
}
```

## Co tento report dokazuje

- vzdálený linked sandbox byl skutečně dotázán přes runner,
- všech 8 definovaných P0.2 databázových kontraktů bylo pravdivých,
- runner použil oddělený read-only token,
- runner neprováděl SQL zápis, migraci, pgTAP write test ani reset databáze,
- výstup neobsahuje token, heslo, connection string ani raw databázová data.

## Omezení

Tento důkaz se týká pouze linked sandboxu. Nepředstavuje důkaz o produkční
databázi a nenahrazuje lokální pgTAP testy. Produkční databáze nebyla použita.
